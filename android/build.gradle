buildscript {
    ext.kotlin_version = '1.8.10' // Specify the correct version you need
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
        // other classpath dependencies
    }
}
